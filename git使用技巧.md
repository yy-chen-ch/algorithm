### 环境配置

```
git config --global user.name     user.email  # 配置信息
git config --lsit                             # 查看配置
git init
git add .
git commit -m "信息"
git remote -v https://github.com/
git pull 
git push -u orgin master

#分支管理
git branch newbranch
git checkout otherbranch
git branch -d delectbranch
git merge branch

```



